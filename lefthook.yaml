pre-commit:
  piped: true
  commands:
    static_code_analysis:
      glob: "*.dart"
      run: flutter analyze
    tests:
      run: flutter test
# pre-push:
#   parallel: true
#   commands:
#     static_code_analysis:
#       run: flutter analyze
